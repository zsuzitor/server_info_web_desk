@model server_info_web_desk.Models.SocialNetwork.Chat

@{
    ViewBag.Title = "Dialog";
}

<div>
    @*див нужен для размещения ссылки при выходе и диалога, что бы по ней перейти нужно потому что мозилла не переходит по ссылке .click()*@
    <div id="div_for_rub"></div>


    <input type="hidden" id="dialog_id_input_id" value="@Model.Id" />
    <div class="dialog_fixed_menu">
        <button onclick="delete_select_messages()">удалить</button>
        <button onclick="">важные</button>
        <button onclick="">переслать</button>
        <button onclick="">запросить удаление дилога</button>
        <button onclick="leave_dialog()">выйти из диалога</button>
        @*<span>@Html.ActionLink("выйти из диалога123", "LeaveDialog", "SocialNetwork",new { id=123 },new { })</span>*@

        <button onclick="">удалить(односторенне удаление)</button>
    </div>
    <div class="div_inline_block div_content_main_size_2 dialog_left_main_block" id="div_content_main_size_2_id">
        
        <div class="div_padding_left_10_px div_padding_top_10_px">
            <button onclick="load_more_messages()">загрузить</button>
            <div id="Dialog_div_message_id">
                
                @Html.Action("ListMessagesUser", "SocialNetwork",new {id=Model.Id, start=0,count=10 })
            </div>
            <div class="div_padding_top_10_px">
                @*@using (Html.BeginForm("AddMemePerson", "SocialNetwork", FormMethod.Post, new { enctype = "multipart/form-data" }))*@
                @*@using (Ajax.BeginForm("SendNewMessageForm", "SocialNetwork", new AjaxOptions {
                    LoadingElementId = "Main_preloader_id", LoadingElementDuration = 200,OnComplete= "OnComplete_SendMessage" }))
                {*@
                    @Html.Hidden("dialog", Model.Id)
                    @Html.TextArea("text",new {@class= "class_for_focus_show" })
                <div class="class_for_focus_show_s">
                    @*<input type="file" name="uploadImage" multiple="multiple" accept="image/*" />*@
                    <button onclick="SendMessage()">Отправить</button>
                    @*<input type="submit" value="Отправить" />

            }*@
                </div>
            </div>
        </div>
    </div>
    <div class="div_inline_block div_content_main_size_1" id="div_content_main_size_1_id"></div>
</div>



@section scripts{
    @Styles.Render("~/Content/Messages.css")
@Scripts.Render("~/scripts/Dialog.js")

}